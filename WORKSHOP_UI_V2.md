# Workshop UI — Spec v2.2

Blade front-end, garage-manager. Αντικαθιστά το `workshop-dashboard-spec.md` (v1.1).
31 Ιουλίου 2026. **Εγκεκριμένο για υλοποίηση.**

> **2.1 → 2.2** — μία διόρθωση, καμία νέα κατεύθυνση.
> Οι ουδέτερες καταστάσεις (`Νέα`, `Ολοκληρωμένη`) γίνονται hairline badges αντί για fill `#F1F1EF`, που ήταν αόρατο πάνω σε λευκή κάρτα (§5). Τίποτα άλλο δεν αλλάζει.

> **2.0 → 2.1** (μετά από review)
> - Αφαιρέθηκε το `< 768px → redirect (middleware)`. Ήταν τεχνικά λάθος: το middleware δεν βλέπει viewport width. Αντικαταστάθηκε από «δεν υποστηρίζεται επίσημα» + σύνδεσμος (§11).
> - Η στενή sidebar γίνεται rail 80px με **ορατά labels**, όχι icons-only με `title` (§2).
> - Η θέση των tokens καταγράφηκε ως αποδεκτό trade-off αντί για ιδανικό (§3).

---

## 0. Τι αλλάζει από την 1.1

Η 1.x κάλυπτε μόνο το dashboard και μόνο σε επίπεδο row/badge. Δούλεψε — αλλά μόλις χτίστηκε η πρώτη φόρμα, φάνηκε ότι έλειπαν δύο ολόκληρα στρώματα: το **shell** (πώς κάθεται η εφαρμογή γύρω από κάθε σελίδα) και η **σύνθεση φόρμας**.

| # | Αλλαγή | Γιατί |
|---|---|---|
| 1 | Top nav → σταθερή sidebar | Το top nav είχε ήδη 4 items και λείπουν 3+ ακόμα. Δεν κλιμακώνεται. |
| 2 | Navy sidebar surface | Το μόνο σημείο που παίρνει οπτικό βάρος χωρίς να ανταγωνιστεί το περιεχόμενο. |
| 3 | Νέο κεφάλαιο: σύνθεση φόρμας (§7) | Η φόρμα «Νέα εντολή» ήταν 4 ισοβαρείς κάρτες χωρίς action bar. |
| 4 | Ρητός κανόνας: χρώμα σε metrics μόνο όπου ζητά ενέργεια | Πρόληψη — 4 έγχρωμα icons στη σειρά είναι το τσίρκο με άλλο όνομα. |
| 5 | Κεφάλαιο regressions (§12) | Τα κεφαλαία επέστρεψαν παρότι ήταν ήδη απαγορευμένα. |

Τα §5 (status colors), §6 (row anatomy) και το `min-width: 0` της 1.1 **ισχύουν αυτούσια** και επαναλαμβάνονται εδώ.

---

## 1. Σκοπός και όρια

Ένας χρήστης: η γραμματέας, σε desktop PC γραφείου, όλη μέρα, με το τηλέφωνο στο αυτί.

Κάθε σελίδα κρίνεται σε ένα ερώτημα: **πόσο γρήγορα απαντάει σε αυτόν που τηλεφωνεί.** Όχι πόσο εντυπωσιακή είναι.

***Εκτός scope:** mobile. Κάτω από 768px η custom workshop οθόνη δεν υποστηρίζεται επίσημα. Το layout υποβαθμίζεται αξιοπρεπώς και εμφανίζεται σαφής σύνδεσμος «Άνοιγμα στο Filament admin». Δεν γίνεται server-side redirect.

---

## 2. Το shell

```
┌────────────┬─────────────────────────────────────────────────────┐
│            │                                                     │
│  Συνεργείο │   Εργασίες                                          │
│            │   Νέα εντολή                        [ Αποθήκευση ]  │
│  ○ Αρχική  │                                                     │
│  ● Εργασίες│   ┌─────────────────────────────────────────────┐   │
│  ○ Πελάτες │   │                                             │   │
│  ○ Οχήματα │   │            περιεχόμενο σελίδας              │   │
│  ○ Ραντεβού│   │                                             │   │
│            │   └─────────────────────────────────────────────┘   │
│  ─────────  │                                                     │
│  ○ Admin   │                                                     │
│            │                                                     │
└────────────┴─────────────────────────────────────────────────────┘
   240px                      flex, max 1100px
   navy                       --ws-page
```

Η sidebar είναι `position: sticky; top: 0; height: 100vh`. Δεν κάνει scroll με τη σελίδα, δεν κρύβεται, δεν ανοιγοκλείνει με κουμπί. Είναι έπιπλο, όχι λειτουργία.

### Τι μπαίνει στη sidebar

Μόνο πλοήγηση. Πέντε προορισμοί, χώρισμα, μετά Admin και αποσύνδεση.

### Τι δεν μπαίνει

- **Το «Νέα εντολή».** Είναι ενέργεια πάνω στο περιεχόμενο, ανήκει στο header της σελίδας. Στη sidebar πάει μόνο πλοήγηση — τη στιγμή που θα δεχτεί το πρώτο κουμπί ενέργειας, θα δεχτεί και το δεύτερο.
- Search. Είναι λειτουργία της αρχικής, όχι καθολική.
- Metrics, badges με μετρητές, «τελευταία δραστηριότητα».

Η sidebar πρέπει να είναι βαρετή. Αυτή είναι η δουλειά της.

### Active state

```css
/* σωστό */
.nav-item[aria-current="page"] {
  background: var(--ws-nav-active);
  color: var(--ws-nav-text-hi);
}
```

Το active item παίρνει **surface** — ένα ελαφρώς ανοιχτότερο navy πίσω από ολόκληρο το item, με το κείμενο σχεδόν λευκό.

Όχι αριστερή έγχρωμη μπάρα (`border-left: 3px solid orange`). Είναι το πιο συχνό tell ότι κάποιος αντέγραψε template, και σπάει τον κανόνα «καμία στρογγυλεμένη γωνία σε μονόπλευρο border».
Όχι πορτοκαλί κείμενο. Το accent είναι για ενέργειες, όχι για θέση.
Όχι bold. Η αλλαγή βάρους μετακινεί το κείμενο κατά ένα pixel σε κάθε hover και φαίνεται.

Το `aria-current="page"` είναι υποχρεωτικό, όχι μόνο η κλάση.

### Στενή κατάσταση (< 1280px)

Η sidebar δεν γίνεται icons-only. Γίνεται **rail 80px με ορατό label κάτω από κάθε εικονίδιο**:

```
┌────────┐
│   ⌂    │
│ Αρχική │
│        │
│   ▤    │
│Εργασίες│   ← active: surface σε όλο το πλάτος του item
│        │
│   ☺    │
│Πελάτες │
└────────┘
```

Icon 20px, label 12px, κεντραρισμένα, item height 56px.

**Γιατί όχι icons-only με `title`:** το `title` attribute δεν είναι προσβασιμότητα. Δεν εμφανίζεται σε touch, δεν εμφανίζεται σε keyboard focus σε αρκετούς browsers, δεν διαβάζεται συνεπώς από screen readers, και έχει καθυστέρηση ~1s που το κάνει άχρηστο για γρήγορη σάρωση. Είναι tooltip για ποντίκι, και μόνο.

**Γιατί όχι tooltip component:** επειδή δεν χρειάζεται. Ένα σωστό tooltip θέλει positioning, escape handling, focus management και ARIA — δηλαδή ένα ολόκληρο component για να λύσει πρόβλημα που δεν έπρεπε να υπάρχει.

Και τα πέντε labels χωράνε σε 80px στα 12px: `Αρχική`, `Εργασίες`, `Πελάτες`, `Οχήματα`, `Ραντεβού`. Το μακρύτερο είναι ~58px. Αν αύριο μπει κάτι που δεν χωράει (`Ανταλλακτικά`), κόβεται σε `Ανταλλ.` — όχι σε εικονίδιο.

Το label μένει πάντα ορατό. Ο χρήστης δεν χρειάζεται να μάθει εικονίδια απ' έξω, και δεν χτίζουμε tooltip infrastructure για να το αποφύγουμε.

---

## 3. Design tokens

Ορίζονται μία φορά στο `layouts/workshop.blade.php`. **Καμία hex τιμή δεν γράφεται σε component.**

> **Γνωστό trade-off, συνειδητά αποδεκτό.** Η σωστή θέση των tokens είναι ξεχωριστό workshop-scoped CSS αρχείο (`resources/css/workshop.css`), όχι `<style>` μέσα σε Blade layout. Το κρατάμε στο layout για να μη μεγαλώσει το scope τώρα.
>
> Ο κανόνας «καμία hex εκτός ενός σημείου» είναι που κάνει τη μετακίνηση φθηνή αργότερα: όταν έρθει η ώρα, είναι ένα copy-paste ενός `:root` block, όχι refactor. Μη σπάσεις τον κανόνα επειδή η θέση δεν είναι ιδανική — ο κανόνας είναι ο λόγος που η θέση μπορεί να αλλάξει.

```css
:root {
  /* επιφάνειες περιεχομένου */
  --ws-page:         #F7F7F5;
  --ws-card:         #FFFFFF;
  --ws-sunken:       #F3F4F6;   /* metric cards, plate, action bar */

  /* κείμενο */
  --ws-text:         #18181B;
  --ws-text-muted:   #6B7280;
  --ws-text-faint:   #9CA3AF;

  /* γραμμές */
  --ws-border:       #E5E7EB;
  --ws-border-hover: #D4D4D8;

  /* sidebar — δικό της, κλειστό σύστημα */
  --ws-nav:          #1C2733;
  --ws-nav-hover:    #24313F;
  --ws-nav-active:   #2B3A4A;
  --ws-nav-text:     #93A4B5;
  --ws-nav-text-hi:  #F1F5F9;
  --ws-nav-divider:  #334456;

  /* accent — δύο σημεία σε όλη την εφαρμογή */
  --ws-primary:      #B0590D;
  --ws-primary-fg:   #FFFFFF;
}
```

Τα `--ws-nav-*` είναι **κλειστό σύστημα**: χρησιμοποιούνται αποκλειστικά μέσα στη sidebar και πουθενά αλλού. Καμία κάρτα, κανένα header, κανένα badge δεν δανείζεται navy.

### Ο κανόνας του accent

Το `--ws-primary` επιτρέπεται σε **ακριβώς δύο σημεία** σε ολόκληρη την εφαρμογή:

1. Το γέμισμα του primary CTA κάθε σελίδας (ένα ανά σελίδα, ποτέ δύο)
2. Το focus ring

Ρητά **όχι** σε: γραμμή ημερομηνίας, section headers, «δες όλα →» links, εικονίδια, active nav item, τίτλους, borders καρτών, required markers.

Το accent είναι σήμανση: όπου κι αν εμφανιστεί, σημαίνει «εδώ πατάς για να ξεκινήσεις κάτι». Αν το φορέσει και μια ημερομηνία, παύει να σημαίνει οτιδήποτε.

---

## 4. Χρώμα στα metrics

**Χρώμα παίρνει μόνο το metric που ζητάει ενέργεια.**

Στο τρέχον dashboard, από τα τέσσερα, ένα μόνο δικαιούται: `ΚΤΕΟ έληξαν`, γιατί συνεπάγεται τηλεφωνήματα σήμερα. Τα υπόλοιπα τρία είναι ουδέτερα.

| | |
|---|---|
| Δεν κάνουμε | έγχρωμο icon ανά metric |
| Δεν κάνουμε | λεπτή έγχρωμη γραμμή στην κορυφή κάθε κάρτας |
| Δεν κάνουμε | τέσσερα διαφορετικά χρώματα στη σειρά |
| Κάνουμε | ουδέτερες κάρτες, χρωματιστό **μόνο το νούμερο** εκείνου που καίει |

Το κριτήριο δεν είναι «φαίνεται ωραίο», είναι **κωδικοποιεί κάτι**. Ένα μπλε εικονίδιο δίπλα στο «Ραντεβού σήμερα: 0» δεν προσθέτει πληροφορία — προσθέτει θόρυβο και υπονομεύει το κόκκινο δίπλα του.

Αν αύριο μπει πέμπτο metric που επίσης ζητάει ενέργεια, παίρνει το ίδιο κόκκινο. Δεν παίρνει καινούριο χρώμα.

---

## 5. Status colors

Ένα ζευγάρι (bg, fg) ανά κατάσταση. Foreground πάντα σκούρο του ίδιου family — ποτέ μαύρο πάνω σε χρωματιστό fill.

| Status | bg | fg | Σημασία |
|---|---|---|---|
| Νέα | *hairline* | `#6B7280` | ουδέτερο — δεν ξεκίνησε τίποτα |
| Σε εξέλιξη | `#E7F0FB` | `#1B5296` | κάποιος δουλεύει πάνω της |
| Αναμονή ανταλλακτικού | `#FBF0DC` | `#7A4A08` | κόλλησε, θέλει ενέργεια |
| Έτοιμη προς παράδοση | `#E8F3E4` | `#33660F` | πάρε τηλέφωνο τον πελάτη |
| Ολοκληρωμένη | *hairline* | `#6B7280` | αρχείο |
| Ληγμένο ΚΤΕΟ | `#FBEBEB` | `#992B2B` | μόνο στη λίστα ΚΤΕΟ |

### Η hairline παραλλαγή

```css
.badge--neutral {
  background: transparent;
  border: 1px solid var(--ws-border);
  color: var(--ws-text-muted);
}
```

Ίδιο σχήμα, ίδιο padding, ίδιο μέγεθος με τα υπόλοιπα badges — μηδέν χρωματική ένταση.

Οι δύο ουδέτερες καταστάσεις είχαν αρχικά fill `#F1F1EF`, που πάνω σε λευκή κάρτα είναι πρακτικά αόρατο. Δίπλα σε badges με ορατό fill, το «Νέα» δεν διαβαζόταν ως ήσυχο badge — διαβαζόταν σαν κείμενο που ξέχασαν να στυλάρουν.

Η ουδετερότητα ήταν σωστή· η υλοποίησή της ήταν λάθος. Το hairline κρατά την πρόθεση (δεν ζητά προσοχή) και επαναφέρει το σχήμα (είναι badge, όχι ελεύθερο κείμενο).

**Το «Νέα» είναι σκόπιμα ουδέτερο** — δεν είναι κατάσταση που ζητά προσοχή, είναι απουσία κατάστασης. Στην 1.0 είχε ξεπλυμένο μπλε-γκρι που δίπλα στο «Σε εξέλιξη» ήταν πρακτικά το ίδιο χρώμα στα 12px.

**Τεστ παλέτας:** τύπωσε τη λίστα ασπρόμαυρη. Αν δεν ξεχωρίζεις τι τρέχει και τι κάθεται, το χρώμα κάνει δουλειά που έπρεπε να κάνει η δομή. Σε λίστα 8 εντολών, το χρωματιστό badge πρέπει να είναι η εξαίρεση.

---

## 6. Τυπογραφία

Ένα family: **Inter**. Δύο βάρη: `400`, `500`. Κανένα `600`/`700`.

| Ρόλος | Μέγεθος | Βάρος | Χρώμα |
|---|---|---|---|
| Τίτλος σελίδας | 28px | 500 | `--ws-text` |
| Metric number | 28px | 500 | `--ws-text` |
| Breadcrumb / eyebrow | 13px | 400 | `--ws-text-muted` |
| Section label | 13px | 500 | `--ws-text-muted` |
| Field label | 13px | 500 | `--ws-text` |
| Row title | 15px | 500 | `--ws-text` |
| Row meta | 13px | 400 | `--ws-text-muted` |
| Badge | 12px | 500 | ανά status |
| Helper / timestamp | 12px | 400 | `--ws-text-faint` |
| Πινακίδα | 13px | 400 | `--ws-text`, mono |
| Nav item | 13px | 400 | `--ws-nav-text` |

### Sentence case. Παντού. Χωρίς εξαίρεση.

`Πελάτης και όχημα` — όχι `ΠΕΛΑΤΗΣ & ΟΧΗΜΑ`.
`Ανοιχτές εντολές` — όχι `ΑΝΟΙΧΤΕΣ ΕΝΤΟΛΕΣ`.
`Γραμμή 1` — όχι `ΓΡΑΜΜΗ 1`.

Δύο λόγοι, και ο δεύτερος είναι ο σοβαρός:

Τα ελληνικά κεφαλαία χάνουν τους τόνους, άρα χάνουν πληροφορία που ο αναγνώστης χρησιμοποιεί για να αναγνωρίσει τη λέξη χωρίς να τη συλλαβίσει. Με `letter-spacing` από πάνω, η λέξη διαλύεται σε γράμματα.

Και: τα κεφαλαία με letter-spacing σε section headers είναι το νούμερο ένα σήμα «enterprise λογισμικό του 2009». Είναι η μοναδική μεγαλύτερη αιτία που μια καθαρή οθόνη μοιάζει με λογιστικό πρόγραμμα.

Επίσης: `&` γράφεται «και».

---

## 7. Σύνθεση φόρμας

Το κεφάλαιο που έλειπε. Ισχύει για κάθε create/edit σελίδα.

### Μία κάρτα, όχι N

Η φόρμα είναι **ένα** αντικείμενο. Οι ενότητες χωρίζονται με hairline dividers μέσα στην ίδια κάρτα, όχι με ξεχωριστές κάρτες που η καθεμία έχει border, header bar και σκιά.

```
✗  [ κάρτα: Πελάτης & όχημα ]
   [ κάρτα: Περιγραφή ]
   [ κάρτα: Κόστος ]
   [ κάρτα: Ανταλλακτικά ]
        → τέσσερα ισοβαρή κουτιά, μηδέν ιεραρχία

✓  ┌ κάρτα ──────────────────┐
   │ Πελάτης και όχημα       │
   │ ───────────────────────  │
   │ Τι δηλώνει ο πελάτης    │
   │ ───────────────────────  │
   │ Κόστος και ανταλλακτικά │
   │ ═══════════════════════  │
   │ [σύνολο]  Ακύρωση  [ΟΚ] │  ← action bar
   └──────────────────────────┘
```

Section label: 13px/500 muted, sentence case, **χωρίς chrome** — όχι background bar, όχι border, όχι εικονίδιο. Μία γραμμή κειμένου με 10px κάτω από αυτήν.

### Action bar

Κάθε φόρμα τελειώνει σε action bar μέσα στην κάρτα, με `--ws-sunken` background:

- Αριστερά: το πιο χρήσιμο υπολογισμένο σύνολο (π.χ. «Σύνολο 0 €»). Αν δεν υπάρχει, κενό.
- Δεξιά: `Ακύρωση` (ghost, muted) και μετά το primary CTA.
- Πάντα ορατή. Αν η φόρμα ξεπερνά το viewport, γίνεται `position: sticky; bottom: 0`.

Το «Αποθήκευση» δεν επιτρέπεται να είναι κάτω από το scroll. Είναι ο λόγος ύπαρξης της σελίδας.

### Destructive actions

Ποτέ γεμισμένο κόκκινο κουμπί μέσα στη ροή της φόρμας.

Το «Καθαρισμός γραμμής», «Διαγραφή» και συναφή είναι **ghost buttons σε `--ws-text-muted`**, με το κόκκινο να εμφανίζεται μόνο στο hover ή στο dialog επιβεβαίωσης.

Ο κανόνας: η πιο καταστροφική ενέργεια της οθόνης δεν επιτρέπεται να είναι το πιο έγχρωμο στοιχείο της. Στην v1 της φόρμας, το «Καθαρισμός» τραβούσε περισσότερο βλέμμα από το «Αποθήκευση».

### Πεδία

| Κανόνας | |
|---|---|
| Ύψος control | 36px, ενιαίο |
| Δύο πεδία δίπλα-δίπλα | μόνο αν είναι σημασιολογικά ζευγάρι (πελάτης + όχημα ✓· περιγραφή + κόστος ✗) |
| Textarea | 2 rows default, auto-grow έως 8. Ποτέ 8 rows κενές. |
| Required marker | `*` σε `--ws-text-faint`, όχι κόκκινο. Το κόκκινο είναι για λάθη που **συνέβησαν**. |
| Helper text | 12px `--ws-text-faint`, κάτω από το πεδίο, όχι πλάγια |
| Disabled πεδίο | δείχνει **γιατί** είναι κλειστό («Πρώτα επιλέξτε πελάτη») — αυτό ήδη γίνεται σωστά |
| Placeholder | πραγματικό παράδειγμα, όχι επανάληψη του label |

Το placeholder «Περιγράψτε το πρόβλημα ή την εργασία που θα εκτελεστεί…» επαναλαμβάνει το label. Γίνεται: «Τριγμός από εμπρός δεξιά κατά την οδήγηση».

### Repeaters (ανταλλακτικά)

Όταν είναι άδειο, φαίνεται **μόνο** το «+ Προσθήκη ανταλλακτικού». Καμία κενή «Γραμμή 1» με τρία toggle buttons και βοηθητικό κείμενο που εξηγεί ότι μπορείς να την αφήσεις κενή.

Αν χρειάζεται να εξηγήσεις στον χρήστη πώς να αφήσει κάτι κενό, δεν έπρεπε να είναι εκεί εξαρχής.

---

## 8. Components

Blade components με typed props. Αν χρειάζονται πάνω από τρία props, είναι δύο components.

```
components/workshop/
├── shell/
│   ├── sidebar.blade.php
│   ├── nav-item.blade.php        # icon, label, route, active
│   └── page-header.blade.php     # eyebrow, title, slot για CTA
├── form/
│   ├── section.blade.php         # label + slot, divider αυτόματα
│   ├── field.blade.php           # label, required, helper, slot
│   └── action-bar.blade.php      # slot αριστερά, slot δεξιά
├── plate.blade.php
├── stat-card.blade.php           # label, value, tone (default|danger)
├── status-badge.blade.php        # παίρνει το enum, τίποτα άλλο
├── section-header.blade.php      # τίτλος + «δες όλα →»
├── work-order-row.blade.php
├── kteo-row.blade.php
└── empty-state.blade.php
```

### Row anatomy (αμετάβλητο από 1.1)

```
[plate]  ┌ όνομα πελάτη            15/500 ┐   [badge]
         └ όχημα · εργασία         13/400 ┘   timestamp 12px faint
```

**Truncation — υποχρεωτικό.** Η μεσαία στήλη: `flex: 1; min-width: 0`. Η meta γραμμή: `overflow: hidden; text-overflow: ellipsis; white-space: nowrap`.

Το `min-width: 0` είναι το σημείο που ξεχνιέται. Ένα flex child έχει by default `min-width: auto`, δηλαδή **δεν συρρικνώνεται κάτω από το περιεχόμενό του** — το `text-overflow` δεν ενεργοποιείται ποτέ και η γραμμή σπρώχνει το badge εκτός κάρτας. Χωρίς αυτό, το `truncate` της Tailwind δεν κάνει απολύτως τίποτα.

Ολόκληρη η γραμμή είναι `<a>`. Hover: `background: var(--ws-sunken)`.

### Icon actions (κλήση, SMS)

`<a href="tel:…">` και `<a href="sms:…">`, όχι `<button>` — δουλεύουν με middle-click, αντιγράφονται, δεν θέλουν JS.
`aria-label` με το όνομα μέσα: «Κλήση Μιχάλη Δημητρίου».
Target ≥ 36×36px.

---

## 9. Status enum

Μία πηγή αλήθειας για label και χρώμα. Το view δεν αποφασίζει τίποτα.

```php
enum WorkOrderStatus: string
{
    case New            = 'new';
    case InProgress     = 'in_progress';
    case AwaitingParts  = 'awaiting_parts';
    case ReadyForPickup = 'ready';
    case Completed      = 'completed';

    public function label(): string { … }
    public function badgeClasses(): string { … }
    public function isOpen(): bool { return $this !== self::Completed; }
}
```

`AwaitingParts` **αντικαθιστά** το `InProgress`, δεν συνυπάρχει. Η αναμονή ανταλλακτικού *είναι* η κατάσταση. Έτσι το διπλό badge λύνεται στη ρίζα, όχι στο view.

Αν χρειαστεί η πληροφορία «είχε ξεκινήσει και κόλλησε», μπαίνει `started_at` timestamp στο model — όχι δεύτερη σημαία κατάστασης.

---

## 10. Δομή αρχείων

```
resources/views/
├── layouts/
│   └── workshop.blade.php        # tokens, Inter, shell, sidebar
├── workshop/
│   ├── index.blade.php
│   ├── work-orders/
│   │   ├── index.blade.php
│   │   ├── create.blade.php
│   │   └── show.blade.php
│   ├── customers/
│   └── vehicles/
└── components/workshop/          # βλ. §8

app/
├── Enums/WorkOrderStatus.php
├── Http/Controllers/Workshop/
└── View/Components/Workshop/
```

Ο controller δίνει έτοιμα collections. Καμία `@php` σε view, κανένα query σε Blade.

---

## 11. Responsive

| Πλάτος | Συμπεριφορά |
|---|---|
| ≥ 1280px | Sidebar 240px πλήρης, content max 1100px |
| 1024–1279px | Sidebar 80px rail — icon + ορατό label (βλ. §2) |
| 768–1023px | Sidebar 80px rail, metric grid σε 2 στήλες |
| < 768px | **Δεν υποστηρίζεται επίσημα.** Το layout στοιβάζεται όσο αντέχει· εμφανίζεται σαφής σύνδεσμος «Άνοιγμα στο Filament admin». |

### Γιατί όχι redirect

Προηγούμενη έκδοση αυτού του spec έγραφε «redirect στο Filament admin (middleware)» κάτω από τα 768px. **Είναι τεχνικά αδύνατο όπως γράφτηκε.**

Το middleware βλέπει το HTTP request. Το request δεν περιέχει viewport width — το πλάτος του παραθύρου υπάρχει μόνο στον browser, μετά το render. Το μόνο που μπορεί να δει το middleware είναι το `User-Agent`, που είναι διαφορετικό πράγμα: λέει «τι συσκευή», όχι «πόσο πλατύ παράθυρο». Ένα iPad σε landscape έχει 1024px και θα έτρωγε redirect· ένας desktop browser σε μισή οθόνη έχει 700px και δεν θα έτρωγε.

Άρα:

- **Κανένα server-side redirect με βάση το μέγεθος.** Δεν υπάρχει σωστή υλοποίηση.
- Το «δεν υποστηρίζεται» υλοποιείται σε CSS: το layout στοιβάζεται, η sidebar γίνεται οριζόντια μπάρα στην κορυφή, και εμφανίζεται ένας σύνδεσμος προς το admin. Δεν εμποδίζουμε κανέναν — απλώς δεν το βελτιστοποιούμε.
- Αν υπάρχει **ήδη** middleware που κάνει UA detection στο repo, αξιολογείται ξεχωριστά πριν αφαιρεθεί. Δεν το θεωρούμε δεδομένο ούτε το γενικεύουμε.

Η μόνη θεμιτή χρήση του UA εδώ είναι ένα προαιρετικό hint («φαίνεται ότι είσαι σε κινητό — το admin δουλεύει καλύτερα εκεί»), ως **σύνδεσμος**, ποτέ ως αναγκαστική ανακατεύθυνση.

---

## 12. Regressions — τι έχει ήδη ξαναγίνει

Κάθε γραμμή εδώ έχει συμβεί μία φορά και διορθώθηκε. Έλεγχος πριν από κάθε PR:

- [ ] **ΚΕΦΑΛΑΙΑ σε section headers.** Επανήλθε στη φόρμα «Νέα εντολή» παρότι ήταν ήδη απαγορευμένο στην 1.0.
- [ ] **Accent σε γραμμή ημερομηνίας.** Το πορτοκαλί βρήκε δρόμο στο «Παρασκευή, 31 Ιουλίου 2026».
- [ ] **Δύο badges στην ίδια γραμμή εντολής.**
- [ ] **Metric label που επαναλαμβάνει section header** («Ανοιχτές εντολές» δύο φορές σε 80px).
- [ ] **`min-width: 0` που λείπει** — φαίνεται ότι το truncate εφαρμόστηκε, δεν κάνει τίποτα.
- [ ] **Server-side λογική με βάση το μέγεθος οθόνης.** Το middleware δεν βλέπει viewport width· κάθε τέτοια απόπειρα καταλήγει σε UA sniffing που σπάει σε tablet.
- [ ] **`title` attribute ως υποκατάστατο label.** Δεν είναι προσβασιμότητα.
- [ ] **Ουδέτερο badge με fill τόσο ανοιχτό που εξαφανίζεται πάνω σε λευκή κάρτα.** Ήσυχο σημαίνει hairline, όχι αόρατο.

---

## 13. Quality floor

Δεν διαπραγματεύεται, δεν αναφέρεται στο UI:

- Ορατό focus ring παντού: `outline: 2px solid var(--ws-primary); outline-offset: 2px`
- Κάθε icon-only action έχει `aria-label` με το όνομα του πελάτη μέσα
- Contrast ≥ 4.5:1 σε κάθε ζευγάρι, badges και sidebar συμπεριλαμβανομένων
- Ελάχιστο font 12px, ελάχιστο click target 36px
- Καμία πληροφορία μόνο με χρώμα — το ληγμένο ΚΤΕΟ λέει «Έληξε 19/04»
- `aria-current="page"` στο active nav item
- `prefers-reduced-motion` σεβαστό· ούτως ή άλλως η μόνη κίνηση είναι hover 120ms

---

## 14. Acceptance checklist

- [ ] Καμία hex τιμή εκτός `layouts/workshop.blade.php`
- [ ] Τα `--ws-nav-*` δεν εμφανίζονται πουθενά εκτός sidebar
- [ ] Το πορτοκαλί εμφανίζεται σε ακριβώς δύο σημεία: primary CTA και focus ring
- [ ] Ένα primary CTA ανά σελίδα
- [ ] Το active nav item έχει surface, όχι μπάρα και όχι χρωματιστό κείμενο
- [ ] Κανένα κουμπί ενέργειας μέσα στη sidebar
- [ ] Sentence case σε κάθε label, header, button και section title
- [ ] Ένα badge ανά γραμμή εντολής
- [ ] «Νέα» και «Σε εξέλιξη» ξεχωρίζουν με μια ματιά, χωρίς ανάγνωση
- [ ] Ένα μόνο metric έχει χρώμα
- [ ] Η φόρμα είναι μία κάρτα με dividers, όχι N κάρτες
- [ ] Το «Αποθήκευση» είναι ορατό χωρίς scroll σε 1366×768
- [ ] Καμία destructive ενέργεια δεν είναι γεμισμένη κόκκινη στη ροή
- [ ] `min-width: 0` σε κάθε flex row με truncated κείμενο
- [ ] Tab μέσα από όλη τη σελίδα: κάθε στάση ορατή
